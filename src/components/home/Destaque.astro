<section class="destaque section">
	<div class="container destaque__inner reveal">
		<p class="eyebrow">Venha fazer parte</p>
		<h2>Uma história para ser contada — e você pode estar nela</h2>
		<p class="destaque__texto">
			Acompanhe os bastidores, as próximas apresentações e tudo que acontece por trás do palco no nosso
			Instagram. E quando quiser ver o que já colocamos em cena, o portfólio está logo ali.
		</p>
		<div class="destaque__cta">
			<a
				href="https://www.instagram.com/t7adoracaoemcena/"
				target="_blank"
				rel="noopener noreferrer"
				class="btn btn-solid"
			>
				Seguir no Instagram
			</a>
			<a href="/portfolio" class="btn">Explorar portfólio</a>
		</div>
	</div>
</section>

<style>
	.destaque {
		text-align: center;
		background: radial-gradient(ellipse at 50% 50%, rgba(140, 29, 43, 0.1), transparent 70%);
	}

	.destaque__inner {
		max-width: 46rem;
		margin-inline: auto;
	}

	.destaque__texto {
		color: var(--color-pearl-dim);
	}

	.destaque__cta {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: var(--space-sm);
		margin-top: var(--space-md);
	}
</style>

<script>
	import { gsap } from "../../scripts/gsap-setup";

	gsap.fromTo(
		".destaque .reveal",
		{ opacity: 0, y: 30 },
		{
			opacity: 1,
			y: 0,
			duration: 0.9,
			ease: "power3.out",
			scrollTrigger: {
				trigger: ".destaque",
				start: "top 80%",
			},
		}
	);
</script>
