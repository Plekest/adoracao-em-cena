<footer class="footer">
	<div class="container footer__inner">
		<img src="/logo.svg" alt="Adoração em Cena" class="footer__logo" width="140" height="50" />

		<nav class="footer__links">
			<a href="/">Início</a>
			<a href="/portfolio">Portfólio</a>
			<a href="/agenda">Agenda</a>
			<a href="/contato">Contato</a>
			<a href="https://www.instagram.com/adoracaoemcena/" target="_blank" rel="noopener noreferrer">Instagram</a>
		</nav>

		<p class="footer__copy">© {new Date().getFullYear()} Adoração em Cena - Grupo de Drama</p>
	</div>
</footer>

<style>
	.footer {
		border-top: 1px solid rgba(201, 166, 107, 0.15);
		padding-block: var(--space-lg);
		background: var(--color-obsidian-soft);
	}

	.footer__inner {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--space-sm);
		text-align: center;
	}

	.footer__logo {
		height: 56px;
		width: auto;
		opacity: 0.85;
	}

	.footer__links {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: var(--space-md);
		font-size: 0.85rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.footer__links a {
		color: var(--color-pearl-dim);
		transition: color 0.3s var(--ease-dramatic);
	}

	.footer__links a:hover {
		color: var(--color-sand);
	}

	.footer__copy {
		font-size: 0.8rem;
		color: var(--color-pearl-dim);
		margin: 0;
	}
</style>
