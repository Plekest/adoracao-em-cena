---
import { getEntry } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";

const peca = await getEntry("portfolio", "juju-e-leleco");
if (!peca) throw new Error("Conteúdo 'juju-e-leleco' não encontrado");

function getYoutubeId(url: string): string | null {
	const watch = url.match(/[?&]v=([^&]+)/);
	if (watch) return watch[1];
	const short = url.match(/youtu\.be\/([^?&]+)/);
	if (short) return short[1];
	return null;
}

const videoId = peca.data.video ? getYoutubeId(peca.data.video) : null;
const videosExtras = peca.data.videos
	.map((url) => getYoutubeId(url))
	.filter((id): id is string => Boolean(id));
---

<BaseLayout title="Prévia D - Juju & Leleco" description="Página de prévia, não indexada e não linkada no site.">
	<div class="preview-banner">Prévia · modelo D - Programa de Teatro · página não linkada no menu</div>

	<section class="palco">
		<div class="hero">
			<p class="flourish reveal">✦ ✦ ✦</p>
			<span class="eyebrow reveal">Ministério Infantil</span>
			<h1 class="title reveal">{peca.data.titulo}</h1>
			<p class="synopsis reveal">{peca.data.sinopse ?? peca.data.descricao}</p>
			{
				peca.data.atores && peca.data.atores.length > 0 && (
					<p class="elenco reveal">
						Estrelando: <b>Juju</b> &amp; <b>Leleco</b> · {peca.data.atores.join(", ")}
					</p>
				)
			}
			<div class="meta-row reveal">
				{peca.data.classificacao && <span class="meta-chip meta-chip--class">{peca.data.classificacao}</span>}
				{peca.data.duracao && <span class="meta-chip">{peca.data.duracao}</span>}
			</div>
			<a href="/contato" class="cta reveal">Ver o espetáculo</a>
		</div>

		<div class="ticket-divider reveal">✦</div>

		<div class="gallery-wrap">
			{
				videoId && (
					<button
						type="button"
						class="featured reveal"
						data-video-trigger
						data-video-id={videoId}
						aria-label={`Assistir teaser de ${peca.data.titulo}`}
					>
						<img src={`https://img.youtube.com/vi/${videoId}/hqdefault.jpg`} alt="" loading="lazy" />
						<span class="play"><svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg></span>
					</button>
				)
			}

			{
				videosExtras.length > 0 && (
					<>
						<p class="gallery-heading reveal">Próximas cenas</p>
						<div class="grid4">
							{videosExtras.map((id) => (
								<button type="button" class="card reveal" data-video-trigger data-video-id={id} aria-label="Assistir vídeo">
									<img src={`https://img.youtube.com/vi/${id}/hqdefault.jpg`} alt="" loading="lazy" />
									<span class="play"><svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg></span>
								</button>
							))}
						</div>
					</>
				)
			}
		</div>
	</section>

	<div class="video-modal" id="video-modal" aria-hidden="true">
		<button type="button" class="video-modal__close" id="video-modal-close" aria-label="Fechar vídeo">
			<svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
				<path d="M6 6L18 18M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
			</svg>
		</button>
		<div class="video-modal__frame" id="video-modal-frame"></div>
	</div>
</BaseLayout>

<style>
	.preview-banner {
		position: relative;
		z-index: 200;
		text-align: center;
		font-size: 0.75rem;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: #0d0b0c;
		background: var(--color-sand);
		padding: 0.5rem 1rem;
	}

	.palco {
		--crimson: #8c1d2b;
		--gold: #c9a66b;
		--pearl: #f2ece4;
		background: var(--color-obsidian);
		font-family: "Playfair Display", Georgia, serif;
		padding-bottom: var(--space-lg);
	}

	.hero {
		max-width: 640px;
		margin: 0 auto;
		padding: var(--space-xl) 1.5rem 0;
		text-align: center;
	}

	.flourish {
		color: var(--gold);
		font-size: 1.1rem;
		margin: 0 0 0.5rem;
		letter-spacing: 0.4em;
	}

	.eyebrow {
		display: inline-block;
		font-family: var(--font-body);
		color: var(--gold);
		letter-spacing: 0.3em;
		font-size: 0.72rem;
		text-transform: uppercase;
		margin-bottom: 1rem;
	}

	.title {
		margin: 0 0 1rem;
		line-height: 1.05;
		font-family: "Playfair Display", Georgia, serif;
		font-style: italic;
		font-weight: 700;
		font-size: 3.4rem;
		color: var(--pearl);
	}

	.synopsis {
		max-width: 52ch;
		margin: 0 auto 1rem;
		font-size: 1.05rem;
		line-height: 1.6;
		color: rgba(242, 236, 228, 0.75);
	}

	.elenco {
		font-family: var(--font-body);
		font-size: 0.85rem;
		color: var(--gold);
		letter-spacing: 0.05em;
		margin-bottom: 1.5rem;
	}

	.elenco b {
		color: var(--pearl);
	}

	.meta-row {
		display: flex;
		gap: 0.6rem;
		justify-content: center;
		flex-wrap: wrap;
		margin-bottom: 1.75rem;
	}

	.meta-chip {
		font-size: 0.78rem;
		padding: 0.3rem 0.85rem;
		font-weight: 600;
		background: transparent;
		border: 1px solid var(--gold);
		color: var(--gold);
	}

	.meta-chip--class {
		background: var(--crimson);
		color: #fff;
		border-color: var(--crimson);
	}

	.cta {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.9rem 1.8rem;
		font-weight: 700;
		font-family: var(--font-body);
		letter-spacing: 0.08em;
		font-size: 0.9rem;
		cursor: pointer;
		border: 1.5px solid var(--gold);
		background: transparent;
		color: var(--gold);
		text-decoration: none;
		text-transform: uppercase;
	}

	.ticket-divider {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		max-width: 640px;
		margin: 0 auto var(--space-lg);
		padding: 0 1.5rem;
		color: rgba(242, 236, 228, 0.3);
		text-align: center;
	}

	.ticket-divider::before,
	.ticket-divider::after {
		content: "";
		flex: 1;
		border-top: 1px dashed rgba(242, 236, 228, 0.3);
	}

	.gallery-wrap {
		max-width: 920px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	.gallery-heading {
		text-align: center;
		font-family: "Playfair Display", serif;
		font-style: italic;
		font-size: 1.2rem;
		margin: 0 0 1.5rem;
		color: var(--gold);
	}

	.featured,
	.card {
		position: relative;
		overflow: hidden;
		display: flex;
		align-items: center;
		justify-content: center;
		cursor: pointer;
		border: 1px solid var(--gold);
		border-radius: 2px;
		padding: 0;
		background: none;
		font: inherit;
		width: 100%;
	}

	.featured {
		aspect-ratio: 16 / 8;
		margin-bottom: 1.5rem;
	}

	.card {
		aspect-ratio: 4 / 3;
	}

	.featured img,
	.card img {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.grid4 {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
		gap: 1rem;
	}

	.play {
		position: relative;
		z-index: 1;
		width: 56px;
		height: 56px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		background: var(--gold);
	}

	.play svg {
		width: 20px;
		height: 20px;
		margin-left: 3px;
	}

	.play svg path {
		fill: #0d0b0c;
	}

	.card .play {
		width: 38px;
		height: 38px;
	}

	.card .play svg {
		width: 14px;
		height: 14px;
	}

	@media (max-width: 720px) {
		.title {
			font-size: 2.1rem;
		}
	}

	/* ---------- Modal de vídeo ---------- */
	.video-modal {
		display: none;
		position: fixed;
		inset: 0;
		z-index: 1000;
		background: rgba(8, 7, 7, 0.94);
		align-items: center;
		justify-content: center;
		padding: var(--space-md);
	}

	.video-modal.is-open {
		display: flex;
	}

	.video-modal__frame {
		position: relative;
		width: min(100%, 1100px);
		aspect-ratio: 16 / 9;
	}

	.video-modal__frame iframe {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		border: 0;
		border-radius: 10px;
		background: #000;
	}

	.video-modal__close {
		position: absolute;
		top: var(--space-sm);
		right: var(--space-sm);
		width: 42px;
		height: 42px;
		border-radius: 50%;
		border: 1px solid rgba(242, 236, 228, 0.3);
		background: rgba(13, 11, 12, 0.6);
		color: var(--color-pearl);
		display: flex;
		align-items: center;
		justify-content: center;
		cursor: pointer;
	}

	.video-modal__close svg {
		width: 18px;
		height: 18px;
	}
</style>

<script>
	import { gsap } from "../scripts/gsap-setup";

	gsap.fromTo(".reveal", { opacity: 0, y: 24 }, { opacity: 1, y: 0, duration: 0.8, stagger: 0.06, ease: "power3.out" });

	const modal = document.getElementById("video-modal");
	const modalFrame = document.getElementById("video-modal-frame");
	const closeBtn = document.getElementById("video-modal-close");

	function closeModal() {
		if (!modal || !modalFrame) return;
		modal.classList.remove("is-open");
		modal.setAttribute("aria-hidden", "true");
		modalFrame.innerHTML = "";
	}

	function openModal(videoId: string) {
		if (!modal || !modalFrame || !videoId) return;
		modalFrame.innerHTML = "";
		const iframe = document.createElement("iframe");
		iframe.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&rel=0`;
		iframe.title = "Vídeo";
		iframe.allow = "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture";
		iframe.allowFullscreen = true;
		iframe.style.cssText = "position:absolute;inset:0;width:100%;height:100%;border:0;border-radius:10px;background:#000;";
		modalFrame.appendChild(iframe);
		modal.classList.add("is-open");
		modal.setAttribute("aria-hidden", "false");
	}

	document.querySelectorAll("[data-video-trigger]").forEach((el) => {
		el.addEventListener("click", () => openModal(el.getAttribute("data-video-id") ?? ""));
	});

	closeBtn?.addEventListener("click", closeModal);
	modal?.addEventListener("click", (e) => {
		if (e.target === modal) closeModal();
	});
	document.addEventListener("keydown", (e) => {
		if (e.key === "Escape") closeModal();
	});
</script>
